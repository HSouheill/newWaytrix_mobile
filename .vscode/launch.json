{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Attach to Android",
            "type": "reactnative",
            "request": "attach",
            // "platform": "android",
            "port": 8081,
            "sourceMaps": true,
            "cwd": "${workspaceFolder}"
        }
    ]
}
